module.exports = {
  printWidth: 90,// Stablish max lenght per line (same in eslint config file)
  singleQuote: true,// Enable single quote instead of double (same in eslint config file)
  jsxSingleQuote: true,// Enforces the use of single quotes for all JSX attribute values that don’t contain a single quote (same in eslint config file)
  trailingComma: 'all',// Enable trailing comas (same in eslint config file)
  bracketSpacing: true,// Enalble to add a space at the start and end of brackets
  tabWidth: 2,// Default identation
  semi: false,// Remove semicolons
  arrowParens: 'always',// Enable to add parentheses in all arrow functions
};
